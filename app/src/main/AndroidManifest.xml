<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application
            android:name=".MyApplication"
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:networkSecurityConfig="@xml/network_config"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.CloudX"
            tools:targetApi="31">
        <service
                android:name=".logic.service.DownloadService"
                android:enabled="true"
                android:exported="false"
                android:foregroundServiceType="dataSync|mediaPlayback">
        </service>

        <activity
                android:name=".ui.settings.SettingsActivity"
                android:exported="false"
                android:label="@string/title_activity_settings"/>
        <activity
                android:name=".ui.login.LoginActivity"
                android:exported="false"/>
        <activity
                android:name=".ui.playList.PlayListActivity"
                android:exported="false"
                android:label="@string/title_activity_play_list"
                android:theme="@style/Theme.CloudX"/>
        <activity
                android:name=".ui.downloadManager.DownloadManagerActivity"
                android:exported="false"
                android:label="@string/title_activity_download_manager"
                android:theme="@style/Theme.CloudX"/>
        <activity
                android:name=".ui.home.MainActivity"
                android:exported="true"
                android:theme="@style/Theme.CloudX">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="app_description_title"
            android:resource="@string/uninstall_tip"/>
        <meta-data
            android:name="app_description_content"
            android:resource="@string/uninstall_content"/>
    </application>

</manifest>